{
  "name": "gruda",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve public/",
    "build": "echo 'Copying files to public/' && cp -r src/* public/ || true",
    "deploy": "netlify deploy --prod --dir=public"
  },
  "dependencies": {
    "parse": "^6.0.0",
    "dotenv": "^16.4.5",
    "@supabase/supabase-js": "^2.45.4",
    "boardgame.io": "^0.50.2"
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-cli": "^1.5.0",
    "serve": "^14.2.3"
  }
}
