import React from 'react';\n\nconst CardList = ({ cards }) => {\n  return (\n    <div>\n      <h3>Your Cards</h3>\n      {cards.length === 0 ? (\n        <p>No cards owned</p>\n      ) : (\n        <ul>\n          {cards.map((card) => (\n            <li key={card.objectId}>\n              {card.cardName} (ATK: {card.atk}, HP: {card.hp})\n            </li>\n          ))}\n        </ul>\n      )}\n    </div>\n  );\n};\n\nexport default CardList;
