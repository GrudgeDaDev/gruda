import ParseService from './ParseService';\n\nconst GameService = {\n  startGame: async (mode, userId, opponentId, gameId) => {\n    return ParseService.runCloudFunction('startGame', {\n      mode,\n      userId,\n      opponentId,\n      gameId,\n    });\n  },\n\n  endGame: async (params) => {\n    return ParseService.runCloudFunction('endGame', params);\n  },\n\n  getLeaderboard: async (gameId, limit = 10) => {\n    return ParseService.runCloudFunction('getLeaderboard', { gameId, limit });\n  },\n};\n\nexport default GameService;
