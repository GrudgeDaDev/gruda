import Parse from 'parse';\nimport ParseService from './ParseService';\n\nconst AuthService = {\n  login: async (username, password) => {\n    return ParseService.runCloudFunction('authenticateUser', {\n      username,\n      password,\n      isSignup: false,\n    });\n  },\n\n  signup: async (username, password) => {\n    return ParseService.runCloudFunction('authenticateUser', {\n      username,\n      password,\n      isSignup: true,\n    });\n  },\n\n  logout: async () => {\n    await Parse.User.logOut();\n    localStorage.removeItem('sessionToken');\n  },\n\n  updateProfile: async (userId, { discord, walletAddress }) => {\n    return ParseService.runCloudFunction('updateProfile', {\n      userId,\n      discord,\n      walletAddress,\n    });\n  },\n};\n\nexport default AuthService;
