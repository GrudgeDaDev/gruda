import ParseService from './ParseService';\n\nconst CardService = {\n  getCards: async (userId = Parse.User.current().id) => {\n    return ParseService.runCloudFunction('manageCards', {\n      action: 'view',\n      userId,\n    });\n  },\n\n  transferCard: async (userId, cardId, targetUserId) => {\n    return ParseService.runCloudFunction('manageCards', {\n      action: 'transfer',\n      userId,\n      cardId,\n      targetUserId,\n    });\n  },\n};\n\nexport default CardService;
